# ConsoCA
Projet en informatique I

```
pip install -r requirements.txt
```

Add your user and password in the corresponding fields in the file config.ini

``` ini
[database]
user = your_user
password = your_password
```

```
python3 src/run.py
```